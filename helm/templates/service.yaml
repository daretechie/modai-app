apiVersion: v1
kind: Service
metadata:
  name: {{ include "modai-app.fullname" . }}
  labels:
    {{- include "modai-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "modai-app.selectorLabels" . | nindent 4 }}
